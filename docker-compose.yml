services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PAYMENT_WEBHOOK_PORT:-8080}:${PAYMENT_WEBHOOK_PORT:-8080}"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./temp:/app/temp
